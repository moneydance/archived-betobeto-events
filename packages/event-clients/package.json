{
  "name": "@betobeto/event-clients",
  "description": "Betobeto event clients",
  "version": "1.0.0",
  "scripts": {
    "prettify": "prettier --write './src/**/*.{ts,tsx}'",
    "test": "jest",
    "test:watch": "jest --watch",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "env-var": "^4.0.0",
    "qs": "^6.7.0"
  },
  "devDependencies": {
    "@types/qs": "^6.5.3"
  }
}
